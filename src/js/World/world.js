// * Set up and maintain parts of the world

import { player } from "../Player/player_old";

export const World = () => {

    function createPlayer() { }

    function createPlayers() { }

    function createDeck() { }

    return {
        createPlayer,
        createPlayers,
    }

}